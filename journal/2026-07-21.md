# 2026-07-21 — プロジェクト発足

## やったこと

- プロジェクト構想を議論し、言語化(n8n ライクなワークフローエンジンを Swift で macOS ネイティブに)
- Swift vs Rust を検討 → **Swift に決定**(macOS アプリという最終形への摩擦が最小。エンジン設計の試行錯誤も structured concurrency で速く回せる)
- プロジェクト名を **Flume** に決定
- **DESIGN.md** を作成(ビジョン、3 つの柱、ドメイン語彙、ADR D1〜D5、フェーズ計画、Swift シグネチャスケッチ、サンプル JSON)
- `swift package init` 済み(Swift 6.3、言語モード v6、library ターゲット `Flume` + `FlumeTests`)
- 開発体制を確立: コードは yosuke が書く。Claude は設計相談・レビュー・答え合わせの伴走

## 決めたこと

- 設計判断は「予想 → 実装 → n8n で答え合わせ」サイクル。ADR に記録(DESIGN.md セクション 5)
- 仕様の本体はゴールデンテスト。prose の仕様書は最小限
- n8n ソースは自分の設計を決めた後にしか読まない(刷り込み防止)

## 学び・発見

- (未検証の予想)素の `AsyncStream` はバッファ無制限でバックプレッシャーがない疑い。
  Flume の憲法の根幹に関わるため、初手のスパイクで検証する

## 次回の一手

1. **スパイク**: 使い捨てコードで AsyncStream の挙動を実測する
   - プロデューサが 100 万アイテム yield、コンシューマはわざと遅く消費 → メモリが平らか?
   - 途中で `Task.cancel()` → 上流は止まるか?
   - 結果を DESIGN.md の ADR に記録(必要なら D6 として「Channel の実装」を起票)
2. スパイクの結論が出たら、**失敗するテストから開始**:
   `decodeHelloFlumeWorkflow`(DESIGN.md 付録 B の JSON をデコード)を書き、
   コンパイルエラーを潰しながら `Workflow` / `Node` / `Connection` / `Value` を型定義する
3. (任意)`flume/` で `git init` して独立リポジトリ化
